import { Routes } from '@angular/router';
import { InvResultComponent } from './inv-result/inv-result.component';

export const routes: Routes = [
    {
        path:'calculate',
        component: InvResultComponent
    }
];
